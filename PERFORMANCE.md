# 手机个人简历性能优化记录

## 具体修复

- 原渲染回调中调用 OrbitControls.update() 会同步触发 change 并重复预约 RAF，导致拖拽后形成多条动画循环。已改成唯一调度入口，移除渲染回调中的控制器更新。
- 默认仅加载简历与静态预览；点击“启动三维演示”后才下载和初始化 Three.js，三个场景首次选择时分别创建。
- 取消环境预滤镜及透射材质，降低几何细分，缓存可复用的几何与材质。
- 移动端 DPR 最高 1.25，低内存设备 1；最高 30fps，暂停、弹层遮盖、离屏、隐藏与上下文丢失停止调度。
- 画布节点拖动合并到每帧一次，使用缓存坐标计算连线，避免每次 pointermove 强制重新布局。
- 手机项目缩略图使用较小 WebP，保留微电网大屏全尺寸图片的独立查看入口。
- 移除自动滚动标语、常驻背景模糊与入场等待，正文立即可读。

## 可复核结果

同一份手机简历功能内容，首次打开加载的 JavaScript（构建后、解压后的文件体积）从 **588,335 字节**降到 **18,500 字节**，减少 **96.9%**。这是首屏加载量，不是全部代码的总体积；三维模块由用户点击后另行加载。

生产预览服务器实际启用 gzip，本次优化版主脚本传输体积为 8,444 字节。旧版基线由不压缩的静态服务器提供，因此上面的前后对比统一使用解压后体积，避免把压缩差异计入优化收益。

390 × 844、设备 DPR 3 的 Chromium 手机模拟测试确认：

- 初始 WebGL 上下文、绘制调用及 RAF 回调均为 0。
- 切换静态预览、阅读经历、打开项目详情不会加载三维模块。
- 启动后，拖拽没有产生多条渲染循环；整页 RAF 待执行计数峰值为 2（包括页面 UI 调度）。
- 暂停、弹层打开、离开首屏三个观察窗口内，RAF 预约数与绘制数均不增长。
- 320 / 390 像素宽度无横向溢出；三个世界、相机重置、履历切换、项目详情、微电网链接与简历 PDF 通过验证。
- 无 JavaScript 异常或请求失败。

测试基于浏览器模拟与计数，不将无头浏览器或软件 GPU 的帧率当作真机性能结论。
